{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/proj2",
            "args": ["1", "2", "3", "4", "5"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Makefile",
            "env": {
                "ASAN_OPTIONS": "detect_leaks=0"
            }
        }
    ]
}
